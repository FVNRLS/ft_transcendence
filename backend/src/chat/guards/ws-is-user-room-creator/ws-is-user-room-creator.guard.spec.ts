import { WsIsUserRoomCreatorGuard } from './ws-is-user-room-creator.guard';

describe('WsIsUserRoomCreatorGuard', () => {
  it('should be defined', () => {
    expect(new WsIsUserRoomCreatorGuard()).toBeDefined();
  });
});
