import { WsIsUserMemberOfRoomForMessageGuard } from "./ws-is-user-member-of-room-for-message.guard";

describe('WsIsUserMemberOfRoomForMessageGuard', () => {
  it('should be defined', () => {
    expect(new WsIsUserMemberOfRoomForMessageGuard()).toBeDefined();
  });
});
