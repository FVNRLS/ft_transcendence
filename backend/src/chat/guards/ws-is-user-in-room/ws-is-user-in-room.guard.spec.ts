import { WsIsUserInRoomGuard } from './ws-is-user-in-room.guard';

describe('WsIsUserInRoomGuard', () => {
  it('should be defined', () => {
    expect(new WsIsUserInRoomGuard()).toBeDefined();
  });
});
